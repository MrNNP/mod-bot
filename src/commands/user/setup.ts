import { Message } from "discord.js"
import ModBot from "../../prototypes/proto";
const questionDiscUser = ModBot.Interact.questionDiscUser;

export default async (msg:Message):Promise<ModBot.DbObjs.guildObj>=>{
  return new Promise<ModBot.DbObjs.guildObj>((resolve, reject) => {
      



   })
}